/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["scontent-ord5-1.cdninstagram.com", "i1.sndcdn.com"],
  },
};

export default nextConfig;
